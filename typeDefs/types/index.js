const { propertyType } = require("./property.type");
const { optionType } = require("./option.type");
const { availabilityType } = require("./availability.type");
const { reservationType } = require("./reservation.type");

module.exports = {
    propertyType,
    optionType,
    availabilityType,
    reservationType
};